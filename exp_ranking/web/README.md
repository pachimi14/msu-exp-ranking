# web

`../bot` が `public/data/rankings.json` を出す。ここはそれを読むだけ。

```powershell
npm install
npm run dev
```

または `run_web_dev_manual.bat`

本番 JSON は Actions が生成。ローカルで 949 人のままなら、bot を回すか Pages の JSON をコピーすること。
